# --- Web framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7
pydantic-settings>=2.2

# --- Utilities ---
pyyaml>=6.0
ruff>=0.4
docker>=7.0

# --- Cassandra (harness schema check) ---
cassandra-driver>=3.29

# --- Spark (harness execution) ---
pyspark==3.5.1

# --- LLM inference ---
# Upper bounds prevent picking up breaking changes in pre-release versions.
transformers>=4.40,<4.52
peft>=0.10,<0.14
bitsandbytes>=0.43
accelerate>=0.30

# --- LangChain ---
langchain-community>=0.2
